# Melsou — Codex Master Execution Command

Read `AGENTS.md` first, then `docs/00_INDEX.md` and every referenced specification. Treat this repository documentation as the authoritative source of truth for Melsou V1.

Audit the repository, then execute `docs/17_IMPLEMENTATION_PLAN.md` in dependency order. In each phase, read the applicable documents; implement the smallest complete production-quality slice; preserve completed work; run the required lint, typecheck, tests, and build; fix failures caused by your changes; and check the phase against its acceptance criteria before progressing.

Do not invent business rules, pricing, external credentials, or physical-print values. Never commit or expose secrets. Use `.env.example` only as placeholders. Do not claim an external integration is complete when running against a mock/development adapter.

Continue through phases without requesting approval merely because a phase finishes. Stop and report the exact missing value, location, completed work, and resume action only if a real credential/account authorization, `TBD_PRINT_VENDOR` value, unsafe production dependency, or material specification conflict prevents safe completion.

Do not mark a phase complete merely because it builds. Completion requires the documented acceptance criteria and required tests to pass. Start now with Phase 1.
