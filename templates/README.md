# Locked V1 template stubs

Each directory pins `template_id` and `version: 1`. These are implementation stubs, not production art or physical print geometry. The Studio must load the declared JSON data; it must not recreate layouts in components. Populate all `TBD_PRINT_VENDOR` geometry only after vendor confirmation.
